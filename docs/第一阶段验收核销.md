# 第一阶段验收核销（核心信号引擎）

> 核销方法：总控文档第 13.3 节 20 条验收条件 + signal-core/plugin-sdk spec 场景
> 逐条映射到实现与测试。冒烟：真实环境启动 25 秒（迁移→激活→插件→Telegram→双链 WS CONNECTED→SIGTERM 优雅退出 0）。

## 文档第 13.3 节验收条件核销

| # | 验收条件 | 实现/证据 |
|---|---|---|
| 1 | 单进程、单 strategy.yaml、单 bot.sqlite | main.py 单入口；config/strategy.yaml；Database 单连接 |
| 2 | 职责边界，无交易凭证 | config.Settings 仅 CoinGecko/Telegram；无 DBotX 依赖 |
| 3 | 双链完全分区、单一 ACTIVE | StrategyFile.solana/bsc 独立模型；strategy_snapshots 唯一 ACTIVE 索引 |
| 4 | 单链单调度器、四模板 next_due_at、Megafilter 注入、三标签不重复 | TemplateScheduler + 002 迁移 + classify_pool（测试 test_discovery_security） |
| 5 | 安全危险/未知禁止信号 | security.py 状态机 + SignalPipeline 安全门禁前置 |
| 6 | PRE_MONITOR_CHECK 先于 G3；PRE_EXECUTION_CHECK 先于落库 | CandidatePipeline 依赖图 + SignalPipeline 顺序 |
| 7 | 事件 Schema v1 与触发条件 | bus/events.py build_signal_created/invalidated + test_bus |
| 8 | 数据保留、容量熔断、Secret、管理员权限 | maintenance.py + test_maintenance；_SecretFilter；_handle_update 管理员校验 |
| 9 | strategy.yaml Schema 全校验 | config.py + test_config（22 用例） |
| 10 | 每链单连接、100 上限、G2 禁用、90/10 容量 | coingecko_ws.py + test_ws；G2 仅探测 |
| 11 | 结果追踪不占实时订阅、计入 REST 并发 | outcome_tracking.py + test_outcome_usage |
| 12 | 候选订阅不被挤占 | WS 准入只按 100 上限+每日预算（无其他主体） |
| 13 | OHLCV 键隔离、无行情可回退 | market_bars 唯一键；StrategyActivationService.rollback |
| 14 | 回测真实 1m 成交、UTC 秒分页、缺口不伪装 | backtest.py + test_backtest |
| 15 | 可信报价地址门禁 | strategy.market_quality.trusted_quote_assets（schema 校验唯一） |
| 16 | 聪明钱双分类、截断不阻挡、链隔离 | strategy.eval_smart_money + test_strategy |
| 17 | 特征公式唯一实现、实时与回测同实现 | strategy.compute_features 共用 |
| 18 | 激活/回退 revision/hash/activation_id | StrategyActivationService + test_backtest |
| 19 | Telegram offset/outbox/nonce 规则 | clients/telegram.py + test_telegram |
| 20 | 三表分层清理与保护 | RetentionCleaner + test_maintenance（未终态保护用例） |

## spec 场景核销

- signal-core 7 条需求场景 → 第 1-8、10-20 条对应实现（职责边界/流水线/事件/结果追踪/手动激活/冻结/Telegram 统一）
- plugin-sdk 7 条需求场景（含新增投递语义、分发超时）→ bus/ + test_bus

## 供应商契约（实测锁定）

- CoinGecko REST 14 项探测通过（fixtures 已录制）；G1/G2/G3 WS 真机 CONNECTED
- GoPlus 双链归一化实测通过
- 三项契约修正已回写文档：G2 字面量 key、include_volume_breakdown 依赖、Top Pools by Token Address
- simple token_price spike 结论锁定：不满足结果标签需求

## 测试统计

- 163 项测试全部通过（单元 140+，含契约 fixture 回放、WS 假服务器、SQLite 集成）
- 冒烟：真实 API 启动 25 秒全链路正常，SIGTERM 退出码 0
